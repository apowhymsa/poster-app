import {createSlice} from "@reduxjs/toolkit";

export const favouriteSlice = createSlice({
    name: 'favourites',
    initialState: [],
    reducers: {
        setFavouriteItem: () => {

        }
    }
})